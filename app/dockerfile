FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml requirements.txt* ./
RUN pip install --no-cache-dir -r requirements.txt || true

COPY app ./app
ENV PYTHONUNBUFFERED=1

# Uvicorn
EXPOSE 8080
CMD ["uvicorn", "app.server_langserve:app", "--host", "0.0.0.0", "--port", "8080"]
